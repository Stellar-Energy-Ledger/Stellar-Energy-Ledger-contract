[package]
name = "afropay-remittance"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21.0", features = ["testutils"] }

[profile.release]
opt-level = "z"
strip = true
debug = false
lto = true
